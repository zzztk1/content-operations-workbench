# 项目2-阿里云部署结果 v1

最后更新：2026-04-12

## 1. 本轮完成

- 成功登录阿里云 ECS：`47.252.94.172`
- 服务器环境确认：
  - Alibaba Cloud Linux 3
  - Docker 26.1.3
  - Docker Compose v2.27.0
- 已上传项目部署包到：`/opt/media-agent`
- 已在服务器侧生成专用 `.env`
  - 复用本地 StepFun / LangSmith 值
  - PostgreSQL 连接已改成容器内网：`postgresql://postgres:postgres@postgres:5432/media_agent`
- 已完成 `docker compose up --build -d`
- 已安装并启用 Nginx
- 已将应用改为：
  - 容器内监听 `127.0.0.1:8000`
  - 由 Nginx 对外代理 `80`

## 2. 当前公网入口

- 健康检查：`http://47.252.94.172/health`
- 体验页：`http://47.252.94.172/demo`
- 工作台：`http://47.252.94.172/studio`
- 接口文档：`http://47.252.94.172/docs`

## 3. 验收结果

### 3.1 公网 surface 验收

结果文件：
- `benchmarks/latest_deployment_surface_report.json`

当前结果：
- `/health`：200
- `/docs`：200
- `/demo`：200
- `/studio`：200
- `/v2/workflow-capabilities`：200

### 3.2 公网工作流 smoke

结果文件：
- `benchmarks/latest_aliyun_public_workflow_smoke.json`

当前结果：
- `status_code = 200`
- 使用公网地址直接请求 `/v2/run` 成功
- 本次 smoke 使用：
  - `image_mock = true`
  - `vision_review_mock = true`
  - 以控制图片与视觉质检成本

## 4. 当前线上模型配置

- 文本模型：`step-3.5-flash`
- 图片模型：`step-2x-large`
- 视觉模型：`step-1o-turbo-vision`
- 当前默认：
  - `IMAGE_MOCK=false`
  - `VISION_REVIEW_MOCK=true`

说明：
- 图片默认真实开启
- 视觉质检默认仍为 mock，以控制线上默认成本
- 前端/请求层可继续做运行时切换

## 5. 安全边界

当前已做：
- PostgreSQL 不再暴露公网端口
- 应用容器不再直接暴露公网 `8000`
- 外部统一通过 Nginx `80` 访问

当前未完成但建议尽快补：
- 当前仍使用 `root + 密码登录` 作为运维入口
- 尚未切换为 SSH 密钥登录
- 尚未关闭密码登录
- 尚未配置 HTTPS

## 6. 结论

项目2当前已经完成：
- 阿里云 ECS 部署
- Docker 容器化上线
- Nginx 反向代理
- 公网可访问
- 公网基本验收

当前已经达到：
- 可交付
- 可容器化
- 可验收
- 可公网演示

剩余工作不再是“能不能上线”，而是：
- 安全加固
- HTTPS
- 运营级监控与长期稳定性收口
