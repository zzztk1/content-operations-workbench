# 项目2最终完成度清单（v2）

最后更新：2026-04-10

## 已完成（可用于最终项目材料）

- 旧版状态机 baseline 保留且可运行：
  - `brief -> research -> writer -> reviewer -> approval -> export`
  - `approve / needs_edit / reject` 三条路径可验证
- 新版 `LangGraph v2` 主链路已落地：
  - 真 `LangGraph StateGraph`
  - 真 `SubGraph`
  - 真 `PostgreSQL Checkpointer`
- 真实模型链路已切到阶跃星辰 `Step Plan`
- 关键节点已接入结构化输出链路：
  - `JSON Mode / 手动解析 / Pydantic`
  - 当前在 `Step Plan` 路线下以 `direct / fenced-salvage` 为主
- 服务治理已落地：
  - `/health`
  - `SlowAPI` 限流
  - 轻量熔断器
  - `graceful shutdown`
- 流式能力已落地：
  - token 级 `SSE` 事件流
  - `/v2/run/stream` 可持续返回 `start / token / update / final`
  - 已捕获 `TTFT`（首字延迟）
- 可观测与排障能力已落地：
  - `request_id`
  - API / engine 双 JSONL 结构化日志
  - 健康检查可返回 provider route / circuit state / postgres connectivity
- 测量材料已落地：
  - v1 baseline 报告
  - v2 mock 报告
  - before / after 对比报告
  - Step Plan 真实链路报告
  - 功能审计报告

## 已完成（补充收口）

- `LangSmith`
  - 已补 tracing 接入点和配置项
  - 已接入真实 key 并新增独立校验脚本
  - 已完成真实远端 tracing 验证
- 结构化输出稳定性
  - 已经不再 fallback
  - 但 `Step Plan` 路线仍以 `fenced-salvage` 为主，不是全部 `direct`

## 未完成（当前不写入最终简历）

- 生产级计费对账
- 多模型动态路由
- 图片/素材并发优化

## 当前结论

项目2现在已经达到“可投递、可演示、可验证”的工程化版本：

- 主线目标已从“状态机 demo”升级为“LangGraph + PostgreSQL”系统
- 核心亮点已有实现、测量、结果文件三件套
- 当前锁定路线已全部收口，后续更多是性能和增强项
