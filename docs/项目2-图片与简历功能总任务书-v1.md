# 项目2 图片与简历功能总任务书 v1

最后更新：2026-04-11

## 1. 目标

这份任务书只做一件事：

把项目2中与**图片能力**相关的功能，以及与**简历亮点**直接相关的功能，统一收口成：

1. 已完成
2. 未完成
3. 还需要评测调优
4. 过程中如果暴露新机会，可以顺手补

执行目标不是继续零散修补，而是：

> 让项目2的图片能力和简历口径尽量收口到“可以演示、可以验证、可以写进简历”的状态。

---

## 2. 图片功能总清单

### 2.1 已完成

1. `visual_subgraph`
2. `plan_cover -> generate_cover`
3. 主图生成
4. 多候选图生成（`cover_candidate_count`）
5. 主图 + 候选图导出
6. `/demo` 展示主图和候选图
7. 请求级 `image_mock` 切换
8. 请求级真实配图切换已修通
9. 真实图片 API 路线已打通
10. 图片质量校验
11. 图片并发 compare 报告
12. 图片模型默认已切到 `step-2x-large`
13. 图文一致性轻量检查（启发式，不调用额外视觉模型）

### 2.2 当前仍未完全收口

1. ~~图片能力还没有并进统一功能审计主报告~~ → 已并入 `benchmarks/audit_feature_coverage.py` 的 `feature_matrix` 与 `image_artifacts`（依赖既有 `latest_*.json` 结果文件）
2. 图文一致性目前只是启发式关键词重合，不是视觉模型级质检
3. ~~还没有基于 `step-2x-large` 的新一轮正式图片性能基线~~ → 已沉淀 `benchmarks/latest_image_performance_baseline.json`（由 `build_image_performance_baseline.py` 从模型切换核验与导出 JSON 汇总，零额外图片 API）；历史条件下的串并行对比见 `latest_image_batch_compare_report.json`，刷新需另跑 `compare_image_batch_modes.py` 并承担成本
4. `/demo` 的图片结果展示还可以继续产品化（已补候选数 / 模型 / 封面延迟等元信息 pill）
5. 图片相关文档和简历边界文档仍在持续对齐（本轮已部分更新）

### 2.3 当前不该硬写成已完成

1. 图片成本下降多少
2. 图片吞吐固定提升多少（除非绑定具体报告和条件）
3. 图文一致性自动质检已闭环
4. 多模型图片路由已完成

---

## 3. 简历亮点总清单

### 3.1 已经可以安全写

1. `LangGraph + SubGraph + PostgreSQL Checkpointer`
2. `MetricsContext` 节点级 metrics
3. `SSE` / token 级事件流 / `TTFT` 捕获
4. `JSON Mode + 手动解析 + Pydantic`
5. `Mock` 开关体系
6. `LangSmith + request_id + structlog / JSONL`
7. `SlowAPI + /health + graceful shutdown + 熔断`
8. before / after 调优与结果文件
9. 真实图片主链路接入
10. 多候选图与导出
11. 图片质量校验
12. 图片并发 compare

### 3.2 需要软化口径

1. 图片并发优化的收益数字要和具体报告绑定
2. 图文一致性只能写“轻量检查”或“启发式检查”
3. 成本相关表述要避免写成明确降本百分比

### 3.3 暂时不要写

1. 图片成本降低多少
2. 图文一致性完全自动化
3. 生产级多模型动态路由

---

## 4. 还需要评测调优的点

### 第一优先级

1. ~~基于 `step-2x-large` 重建图片性能基线~~（已完成：`latest_image_performance_baseline.json`；吞吐类对比仍以 `latest_image_batch_compare_report.json` 为准并注意模型版本）
2. ~~把图片能力并入统一功能审计~~（已完成：`audit_feature_coverage.py` + `validate_image_audit_artifacts.py`，含基线行）
3. 校验所有图片相关结果文件、demo、导出字段是否一致（持续：`validate_image_audit_artifacts` + 功能审计中的 `image_artifacts`）

### 第二优先级

4. 继续优化 `/demo` 的图片区展示
5. 提升图文一致性结果的可读性和可用性
6. 检查是否还有图片相关的乱码、旧模型名、旧文案残留

### 第三优先级

7. 如果过程中暴露出明显低风险的图片相关 bug，直接修复
8. 如果能低成本补图片侧 before / after，优先补

---

## 5. Cursor 执行要求

请 Cursor `auto` 以 **agent 模式**执行，不要只给建议。

目标：

1. 先基于当前项目状态，复核这份任务书中的“已完成 / 未完成 / 需调优”判断是否准确
2. 把图片功能和简历口径继续往“可演示、可验证、可写”推进
3. 过程中如果暴露出值得顺手做的低风险项，直接一起做

执行边界：

1. 优先做图片相关功能、验证、文档收口
2. 可以修改代码、脚本、demo、文档
3. 优先跑低成本验证
4. 只有在必要时才做最小真实图片验证
5. 不要无意义重复跑高成本真实批量图片 benchmark
6. 不要改坏现有文本主链路

请输出：

1. 最终完成了哪些图片功能
2. 还剩哪些图片项没有完成
3. 新增/修改了哪些文件
4. 跑了哪些验证
5. 哪些简历点因此可以升级，哪些仍然不能写过头

---

## 6. 我方最终验收标准

1. 图片功能现状判断更清楚，不再混乱
2. 至少补完一轮“图片能力 + 简历口径 + 结果文件”的统一收口
3. 如果有新增实现，必须有对应验证
4. 如果有收益表达，必须能落到具体报告
